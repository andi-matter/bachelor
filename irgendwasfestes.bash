

echo ./*
	
	for dir in ./*; do
		printf $dir 
		printf "\n ylgsodfij"
		# if [[ $dir == *"${runNumber}"* ]]; then
			# memDir = $dir
			# printf "memDir "
			# printf $memDir
			# for file in "${memDir}/*"; do
				# echo " test here   "
				# printf $file
				# if [[ $file == *".root"* ]]; then
					# memFile = $file
					# printf "rootfile found  "
					# printf $memFile 
					# break
				# fi 
			# done 			
		# fi
	done 